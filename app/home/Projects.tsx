import { lilita_One } from "@/ui/fonts";

const Projects = () => {
  return (
    <section className={"mt-[100px] "}>
      <header
        className={`text-[30px]  lg:text-[70px] ${lilita_One.className} text-center text-[120px]`}
      >
        Projects
      </header>
    </section>
  );
};

export default Projects;
